import SectionTitle from "../shared/SectionTitle";

const PopularItems = () => {
    return (
        <div>
            <SectionTitle
                heading="Popular Products"
                subHeading="Discover the Trending and Favorite Products"
            />
        </div>
    );
};

export default PopularItems;
